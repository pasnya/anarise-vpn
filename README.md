# ANARISE VPN - Бесплатный VPN для Андроид и Windows (NaiveProxy, Hysteria2, VLESS, Mieru)

## VPN-клиент со встроенными публичными конфигурациями

**ANARISE VPN** - это легкий, быстрый и полностью бесплатный VPN-клиент с открытым исходным кодом для Android и Windows. Приложение разработано для надежного обхода блокировок (DPI) и защиты приватности в сети с помощью передовых протоколов маскировки трафика.

---

## Ключевые поисковые запросы / Keywords

Для быстрого поиска проекта используются следующие ключевые слова:
*   `бесплатный vpn для андроид`, `free vpn for android`, `vpn клиент windows`, `vpn client windows`, `naiveproxy client`, `naiveproxy android client`, `hysteria2 vpn`, `обход блокировок`, `обход DPI`.
*   `vless android клиент`, `naiveproxy андроид клиент`, `vpn без рекламы`, `раздельное туннелирование vpn`, `обфускация трафика salamander`, `mieru client`, `mieru client windows`, `mieru android client`.

---

## Структура репозитория

Репозиторий разделен на две основные части:

*   📂 [/android](file:///d:/Сайты/Anarise/android) — Мобильный клиент под Android (Kotlin, Jetpack Compose, Material 3, vyom-tun-sdk на Go/C++, ядра Hysteria2 и Mieru).
*   📂 [/windows](file:///d:/Сайты/Anarise/windows) — Десктопный клиент под Windows (.NET 8.0 WPF, WebView2 HTML/CSS/JS frontend, управление ядрами Xray-core, Hysteria2 и Mieru).

---

## Клиент Android (/android)

Полноценный и быстрый **бесплатный vpn для андроид** с графическим интерфейсом. Это надежный **naiveproxy android client**, **vless android клиент** и **mieru android client**, поддерживающий обход современных систем DPI.

### Основные фичи
*   **Поддержка NaiveProxy** — обход блокировок с маскировкой под стандартный трафик Chrome (HTTP/2).
*   **Hysteria2 & VLESS** — высокая скорость, низкая задержка и поддержка обфускации **Salamander**.
*   **Mieru** — обход блокировок по протоколу Mieru (TCP/UDP).
*   **Вкладка «Внешние»** — автоматическая загрузка, параллельная проверка пинга и фильтрация публичных серверов.
*   **Раздельное туннелирование (Split Tunneling)** — выбор приложений, работающих через VPN.
*   **Быстрый пинг** всей истории в один клик.

### Сборка
Требуются Android SDK и Java 17.
```bash
cd android
./gradlew assembleRelease
```
Скомпилированный APK появится по пути: `app/build/outputs/apk/release/app-release.apk`.

---

## Клиент Windows (/windows)

Легковесный десктопный **vpn клиент windows** на базе .NET 8 WPF, использующий WebView2 для отображения интерфейса.

### Основные фичи
*   **Системный прокси-режим** — автоматическое переключение системных настроек прокси Windows (SOCKS5/HTTP на localhost) при подключении. Не требует прав администратора.
*   **Автозагрузка ядер** — при первом запуске приложение само скачивает стабильные бинарные сборки `xray.exe`, `hysteria.exe` и `mieru.exe` с официальных релизов на GitHub.
*   **Интерактивный лог** — вывод логов работы ядер в реальном времени прямо на вкладке логов.
*   **Автозапуск** и сворачивание в системный трей.
*   **Внешние списки** с автоматической фильтрацией и сортировкой по TCP-пингу.
*   **Поддержка Mieru** — обход блокировок с использованием протокола Mieru.
*   **Server Explorer** — поиск серверов, фильтрация по регионам и отображение состояния защиты.
*   **Надёжная установка ядер** — загрузка Windows-модулей с увеличенным таймаутом для медленных CDN и прокси-соединений.

### Сборка
Требуется .NET 8.0 SDK.
```powershell
cd windows
dotnet publish -c Release -r win-x64 --self-contained false
```
Результат сборки будет находиться в папке `bin/Release/net8.0-windows/publish/`.

---

*Разработано с душой для свободного и быстрого интернета. Пользуйтесь на здоровье!*
